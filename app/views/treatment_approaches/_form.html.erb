<%= form_with(model: treatment_approach) do |form| %>
  <% if treatment_approach.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(treatment_approach.errors.count, "error") %> prohibited this treatment_approach from being saved:</h2>

      <ul>
        <% treatment_approach.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
