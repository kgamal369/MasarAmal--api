<%= form_with(model: mental_service) do |form| %>
  <% if mental_service.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mental_service.errors.count, "error") %> prohibited this mental_service from being saved:</h2>

      <ul>
        <% mental_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
