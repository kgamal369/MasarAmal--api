<%= form_with(model: cms_value) do |form| %>
  <% if cms_value.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cms_value.errors.count, "error") %> prohibited this cms_value from being saved:</h2>

      <ul>
        <% cms_value.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
